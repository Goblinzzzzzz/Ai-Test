[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "cd ai-assessment-app",
  "npm ci --include=dev",
  "cd server && npm ci --include=dev"
]

[phases.build]
cmds = [
  "cd ai-assessment-app",
  "npm run build"
]

[start]
cmd = "cd ai-assessment-app && npm start"
